<!doctype html>

<html lang="ar" dir="rtl">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>عد تنازلي — حتى 20 سبتمبر 2025</title>
  <style>
    :root{
      --bg:#0f1724;
      --card:#0b1220;
      --accent:#06b6d4;
      --muted:#9aa4b2;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      font-family: "Cairo", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background:linear-gradient(135deg,var(--bg), #081122 70%);
      color:#e6eef6;
      padding:20px;
    }
    .card{
      background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border-radius:16px;
      padding:28px;
      width:min(850px,98%);
      box-shadow:0 10px 30px rgba(2,6,23,0.6);
      border:1px solid rgba(255,255,255,0.03);
    }
    .header{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:12px;
      margin-bottom:18px;
    }
    .title{
      font-size:20px;
      font-weight:700;
      letter-spacing:0.2px;
    }
    .subtitle{color:var(--muted); font-size:13px}.target{
  background:rgba(255,255,255,0.02);
  padding:8px 12px;
  border-radius:8px;
  font-size:13px;
  color:var(--accent);
  font-weight:600;
}

.countdown{
  display:grid;
  grid-template-columns:repeat(4,1fr);
  gap:14px;
  margin-top:12px;
}
@media (max-width:560px){
  .countdown{grid-template-columns:repeat(2,1fr)}
}
.unit{
  background:linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01));
  border-radius:12px;
  padding:18px;
  text-align:center;
  border:1px solid rgba(255,255,255,0.02);
  min-width:120px;
}
.number{
  font-size:44px;
  font-weight:800;
  line-height:1;
  letter-spacing:0.5px;
}
.label{margin-top:6px; color:var(--muted); font-size:13px; font-weight:600}

.footer{margin-top:16px; text-align:center; color:var(--muted); font-size:13px}
.small{font-size:12px; color:#aab6c2}

.pulse{animation:pulse 1.2s infinite}
@keyframes pulse{0%{transform:scale(1)}50%{transform:scale(1.02)}100%{transform:scale(1)}}

.done{display:flex;flex-direction:column;align-items:center;gap:10px;padding:24px}
.done h2{margin:0;color:var(--accent)}

  </style>
</head>
<body>
  <div class="card">
    <div class="header">
      <div>
        <div class="title">عد تنازلي حتى 20 سبتمبر 2025</div>
        <div class="subtitle">يعرض بالأيام والساعات والدقائق والثواني (محدث بالزمن المحلي للجهاز)</div>
      </div>
      <div class="target" id="targetLabel">20 سبتمبر 2025 — 00:00:00</div>
    </div><div id="countArea">
  <div class="countdown" aria-live="polite">
    <div class="unit pulse">
      <div class="number" id="days">0</div>
      <div class="label">أيام</div>
    </div>
    <div class="unit pulse">
      <div class="number" id="hours">0</div>
      <div class="label">ساعات</div>
    </div>
    <div class="unit pulse">
      <div class="number" id="minutes">0</div>
      <div class="label">دقائق</div>
    </div>
    <div class="unit pulse">
      <div class="number" id="seconds">0</div>
      <div class="label">ثواني</div>
    </div>
  </div>
</div>

<div class="footer">
  <div class="small">الوقت يحسب بحسب توقيت جهاز المستخدم المحلي. عند الوصول إلى التاريخ سيُعرض تنبيه.</div>
</div>

  </div>  <script>
    // الهدف: منتصف ليل 20 سبتمبر 2025 بالتوقيت المحلي للجهاز
    const target = new Date(2025, 8, 20, 0, 0, 0); // شهر سبتمبر = 8 (0-based)

    const daysEl = document.getElementById('days');
    const hoursEl = document.getElementById('hours');
    const minutesEl = document.getElementById('minutes');
    const secondsEl = document.getElementById('seconds');
    const countArea = document.getElementById('countArea');
    const targetLabel = document.getElementById('targetLabel');

    function pad(n){return String(n).padStart(2,'0');}

    function update(){
      const now = new Date();
      let diff = target - now; // بالميلي ثانية

      // إذا وصلنا أو تجاوزنا التاريخ
      if(diff <= 0){
        // نعرض رسالة "وصلنا للتاريخ" مع الوقت السلبي (مر عليه)
        countArea.innerHTML = `
          <div class="done">
            <h2>وصلنا إلى 20 سبتمبر 2025 🎉</h2>
            <div class="small">يمكنك تعديل التاريخ أو إعادة تشغيل العد إذا رغبت.</div>
          </div>
        `;
        clearInterval(intervalId);
        return;
      }

      const sec = Math.floor(diff / 1000);
      const days = Math.floor(sec / (3600*24));
      const hours = Math.floor((sec % (3600*24)) / 3600);
      const minutes = Math.floor((sec % 3600) / 60);
      const seconds = Math.floor(sec % 60);

      daysEl.textContent = days.toLocaleString('en-US');
      hoursEl.textContent = pad(hours);
      minutesEl.textContent = pad(minutes);
      secondsEl.textContent = pad(seconds);
    }

    // تحديث فوري ثم كل ثانية
    update();
    const intervalId = setInterval(update, 1000);

    // للمطور: إذا أردت أن يكون الوقت حتى 23:59:59 بدل بداية اليوم، غيّر target إلى (2025,8,20,23,59,59)
  </script></body>
</html>
